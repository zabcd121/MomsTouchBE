spring:
  config:
    import: application-filedir.yml
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3305/MomsTouch?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: love1994
    hikari:
      maximum-pool-size: 40
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true #To beautify or pretty print the SQL
        default_batch_fetch_size: 100
        #show_sql: true #show sql

server:
  port: 8071